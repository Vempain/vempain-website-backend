spring:
  config:
    activate:
      on-profile: prod

logging:
  level:
    root: INFO
    fi.poltsi.vempain.website: INFO

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
